# yuiyuiRPG コーディングガイド

## 1. 目的

この文書は、`yuiyuiRPG` に参加する開発者が同じ判断基準でコードを書き、レビューするためのプロジェクト規約です。

本プロジェクトでは、きれいなコードを次のように定義します。

> 意図と責務が第三者に伝わり、変更の影響範囲を予測でき、動作を検証しながら安全に変更できるコード

コードの短さや高度な構文の使用は、それ自体を品質とはみなしません。次の4つを品質の判断軸とします。

| 判断軸 | 確認すること |
| --- | --- |
| 理解容易性 | 名前、配置、処理の流れから意図を読み取れるか |
| 変更容易性 | 仕様変更の際に、変更すべき場所と影響範囲を予測できるか |
| 一貫性 | 同じ概念が同じ名前、書式、構造で表現されているか |
| 検証可能性 | 変更後の正しさを構文確認や操作確認で確かめられるか |

この文書は JavaScript、HTML、CSS、JSON、およびそれらに関係するドキュメントへ適用します。既存コードに規約との差がある場合は、依頼と無関係な箇所を一括変更せず、変更する範囲から段階的にそろえます。

## 2. 規則の強さ

本書では規則の強さを次の3段階で表します。

- **必須**：構文、安全性、プロジェクト構造に関わるため、原則として守る。
- **推奨**：多くの場合に保守性を高める。採用しない場合は、その方が明確になる理由を説明できるようにする。
- **任意**：状況に応じて採用する。導入自体を目的にしない。

例外が必要な場合は、単に規則を無視するのではなく、コードまたは関連文書に理由、影

### 必須

- `Game` は進行を決定します。表示のための直接的なDOM操作は `Screen` に置きます。
- `Screen` は表示を担当します。ダメージ計算や勝敗条件などのゲームルールを持たせません。
- `InputController` は入力を通知します。入力後に何が起きるかは `Game` が決定します。
- HTMLは構造、CSSは見た目、JavaScriptは入力・状態変化・描画の制御を担当します。

### 判断例

- ダメージ式を変える → `BattleSystem.js`
- 勝利後の進行を変える → `Game.js`
- ステータスの表示項目を変える → `Screen.js`
- 戦闘画面の配置を変える → `style.css`
- JSONの項目をモデルへ変換する → `src/data/`

1つの変更が多くの層にまたがること自体は問題ではありません。ただし、各層にはその層の責務に属する変更だけを置きます。

## 4. JavaScript

### 4.1 命名

名前は、新しく参加した開発者が周辺コードを詳しく読まなくても役割を推測できることを優先します。

#### 必須

- クラス名は PascalCase(先頭大文字) にする：`BattleSystem`、`InputController`
- 関数名と変数名は camelCase にする：`handleSkill()`、`selectedCharacter`
- 関数名は原則として動詞または動詞句にする：`renderBattle()`、`createRandomEnemy()`
- 配列やコレクションは複数形にする：`characters`、`usableSkills`
- 真偽値は質問として読める名前を使う：`isAlive`、`canUseSkill`、`hasReward`
- 同じ概念には同じ用語を使う。本プロジェクトでは敵を `enemy` と表し、同じ意味で `monster` などを混在させない。

### 4.2 変数と状態変更

#### 必須

- 再代入しない変数は `const`、再代入が必要な変数だけ `let` を使います。
- `var` は使いません。
- HPやMPなどモデル自身の状態を変更する既存メソッドがある場合は、呼び出し側から直接書き換えず、そのメソッドを使います。
- 状態を変更する処理と、画面へ表示する処理を同じ関数へ無秩序に混在させません。

```js
defender.takeDamage(damage);
user.useMp(skill.costMp);
```

### 4.3 関数の責務

#### 推奨

関数は、関数名に沿った1つの目的を持たせます。行数だけを根拠に分割せず、次の兆候があるときに分割を検討します。

- 関数名と直接関係しない処理が含まれる
- 複数の異なる理由で同じ関数を変更している
- 処理の段階を長いコメントで説明しなければ追えない
- 同じ処理を別の場所でも利用する必要がある
- 条件分岐の入れ子が深く、正常系を追いにくい

一方で、1か所でしか使わない数行の処理を名前の弱い関数へ分けると、読む場所だけが増えることがあります。分割後の方が意図を説明しやすい場合に分けます。

### 4.5 重複と共通化

#### 推奨

同じ知識やルールを表し、同じ理由で一緒に変更される処理は1か所へまとめます。

本プロジェクトでの例：

- プレイヤーと敵に共通するステータスHTML → `Screen.createStatusHtml()`
- 画面クラスの切り替え → `Screen.setScreenClass()`
- プレイヤー行動後の勝敗判定と敵ターン → `Game.handleBattleAfterPlayerAction()`

見た目が似ているだけで将来別々に変わる処理は、無理に共通化しません。複雑な引数や条件分岐が増え、呼び出し側から目的が分かりにくくなる共通化は避けます。

### 4.6 固定値

#### 必須

- フェーズ名のように、複数箇所で同じ集合を参照する値は定数へまとめます。
- 同じ文字列や数値のタイプミスがゲーム進行を壊す場合は、直接記述を繰り返しません。

#### 推奨

ゲームバランスに関わり、調整対象になる数値には意味の分かる名前を付けるか、設定データへまとめます。

```js
const battleSettings = {
  minimumDamage: 1,
  bossWinStreak: 10,
  criticalRate: 0.1,
};
```

`0` 件との比較や配列の先頭を示す `0` など、文脈から意味が明らかな値まで定数化する必要はありません。

### 4.7 書式

#### 必須

- インデントは半角スペース2つにする。
- 文末にセミコロンを付ける。
- カンマの後へ空白を入れる。
- ファイルはUTF-8で保存する。
- import文ではファイル名の大文字・小文字を実ファイルと一致させる。

書式は内容を読むための共通基盤です。将来formatterを導入した場合は、その設定を本節より優先し、本書と設定が矛盾しないよう更新します。

## 5. データ境界とエラー処理

### 必須

- `dataset` の数値は、配列番号や計算に使う前に `Number()` で変換する。
- 配列番号から取得したキャラクターやスキルが存在するか確認する。
- 戦闘、報酬などの操作は、対応する `GamePhase` でだけ実行する。
- HPとMPは定められた下限・上限を超えないようにする。
- `fetch()` の失敗や不正なJSONによって起動できない場合は、原因を確認できる形で扱う。

```js
const index = Number(input.skillIndex);
const selectedSkill = player.skills[index];

if (!Number.isInteger(index) || !selectedSkill) {
  return;
}
```

### 推奨

- 回復不能な起動エラーと、MP不足のような通常のゲーム結果を区別します。
- ユーザーが選び直せる失敗は、開発者コンソールだけでなく画面上にも理由を表示します。
- エラーを握りつぶす空の `catch` は使いません。

## 6. HTML

HTMLは、見た目ではなく文書と操作の意味が伝わる構造にします。意味のあるHTMLはアクセシビリティを高め、CSSやJavaScriptからも扱いやすい基盤になります。

### 必須

- `button`、`main`、`section` など、目的に合う要素を使う。
- 操作にはクリック可能な `div` ではなく `button` を使う。
- フォーム送信を目的としないボタンには `type="button"` を付ける。
- 同じ文書内で `id` を重複させない。
- 画像には用途に合う `alt` を付ける。装飾だけの画像は空の `alt=""` を検討する。
- JavaScriptへ渡す操作名や番号は `data-*` 属性に置く。
- 属性の `=` の前後に空白を入れない。
- 親子関係が分かるよう半角スペース2つでインデントする。

```html
<button
  type="button"
  class="game-action-button"
  data-action="use-skill"
  data-skill-index="2"
>
  スキル
</button>
```

`index.html` には継続して使うDOMの土台を置き、キャラクター数やスキル数によって変わる要素は `Screen` が生成します。

## 7. CSS

CSSは、共通の見た目、画面固有の見た目、画面幅による差を区別します。

### 必須

- JavaScriptから大量のインラインスタイルを指定せず、クラスを切り替えてCSSへ見た目を任せる。
- 画面の表示状態は `title-screen`、`player-select-screen`、`battle-screen`、`reward-screen` などbodyの画面クラスを基準にする。
- 同じbodyに複数の画面クラスを残さない。
- セレクターは必要以上に具体的にせず、既存のルールを上書きするためだけに詳細度を上げ続けない。

### 推奨

- 全画面共通、タイトル、キャラクター選択、戦闘、報酬、メディアクエリの順に関連ルールをまとめます。
- 複数の部品で同じ意味を持つ見た目は共通クラスへ置き、固有クラスでは差分だけを指定します。
- 繰り返し使い、同時に変更すべき色や余白はCSSカスタムプロパティへまとめます。

```html
<button class="menu-card character-select-card">
<button class="menu-card reward-card">
```

```css
.menu-card {
  border: 2px solid var(--color-accent);
  border-radius: 10px;
}

.character-select-card {
  min-height: 145px;
}

.reward-card {
  min-height: 150px;
}
```

似ている値をすべて変数化する必要はありません。同じ意味を持ち、一緒に変更する値だけをまとめます。

### 推奨

コメントには次の内容を残します。

- 仕様上、その処理が必要な理由
- ブラウザや外部データ形式による制約
- 単純に見える方法を採用しなかった理由
- 一時的な回避策の背景と解消条件

```js
// datasetの値は文字列なので、配列番号として使う前に変換する。
const skillIndex = Number(input.skillIndex);
```

次のような、コードと同じ内容を繰り返すコメントは原則として書きません。

```js
// 連勝数を1増やす。
winStreak += 1;
```

コードを変更したときは周辺のコメントと関連文書も確認します。内容と一致しないコメントは、コメントがない状態より誤解を招きます。

## 9. リファクタリング

リファクタリングは、外部から見える動作を保ったまま内部構造を改善する変更です。機能追加や仕様変更とは目的を分けます。

### 必須

- 依頼と無関係な既存変更を戻さない。
- 1回の変更で、書式変更、名前変更、構造変更、機能変更を無制限に混在させない。
- 変更前後で、対象機能が同じように動くことを確認する。

### 推奨

1. 変更の目的と対象範囲を決める。
2. 変更前の動作を確認する。
3. 小さな単位で構造を変更する。
4. 構文確認と対象機能の確認を行う。
5. 次の変更へ進む。

将来必要になるかもしれない機能のために、現在使われない抽象化を先に追加しません。現在の要件を明確にする最小限の設計を優先します。

## 10. 変更後の検証

コードが読みやすくても、動作を確認できなければ安全には変更できません。変更内容に応じて、最低限次を確認します。

### JavaScriptの構文

```powershell
node --check src/core/Game.js
node --check src/ui/Screen.js
```

変更した他のJavaScriptファイルにも `node --check` を実行します。

### 主要導線

1. タイトルからキャラクター選択、戦闘へ進める。
2. 通常攻撃とスキル成功が動作する。
3. MP不足時に敵ターンへ進まず、理由が表示される。
4. 勝利後に報酬を選び、次の戦闘へ進める。
5. 敗北後にタイトルへ戻れる。

### 境界条件と表示

- プレイヤーまたは敵が自傷スキルで倒れた場合の勝敗
- HPとMPの下限・上限
- 連勝数と戦闘メッセージの改行
- プレイヤーと敵の画像および向き
- bodyに複数の画面クラスが残っていないこと
- パソコン幅とスマートフォン幅の表示
- ブラウザの開発者ツールConsoleに予期しないエラーがないこと

文書だけの変更ではゲーム全体の操作確認は必須ではありません。ただし、文書中のコマンド、ファイル名、クラス名、仕様が実装と一致することを確認します。

## 11. コードレビューの確認項目

### 設計

- [ ] 変更は適切な責務のファイルに置かれている
- [ ] 変更箇所と影響範囲を説明できる
- [ ] 現在使われない仕組みを先回りして追加していない

### JavaScript

- [ ] 名前からデータや処理の役割を推測できる
- [ ] 同じ概念に同じ用語を使っている
- [ ] 関数に異なる責務が混在していない
- [ ] 条件分岐の正常系を追いやすい
- [ ] 外部入力を境界で変換・検証している
- [ ] 同じルールを複数箇所へ重複して実装していない

### HTML・CSS

- [ ] HTML要素が用途に合っている
- [ ] ボタン、`alt`、`id`、`data-*` 属性が規約に沿っている
- [ ] 見た目はCSSが担当している
- [ ] 共通スタイルと画面固有スタイルの差が明確である
- [ ] セレクターの詳細度を不必要に上げていない

### コメント・検証

- [ ] コメントは処理の言い換えではなく理由や制約を説明している
- [ ] 変更したコードと関連文書が一致している
- [ ] 構文確認と、変更に応じた操作確認を行った
- [ ] Consoleに予期しないエラーがない

## 12. 参考資料

本書は次の公開資料で示されている考え方を参考に、本プロジェクトの規模と構成に合わせて具体化しています。外部ガイドをそのまま適用するのではなく、本書をこのリポジトリの規約とします。

- [Google Style Guides](https://google.github.io/styleguide/)：共同開発における一貫した規約
- [Google JavaScript Style Guide](https://google.github.io/styleguide/jsguide.html)：命名、書式、JavaScriptの記述規則。更新停止済みのため、現在も妥当な一般原則だけを参考にしています。
- [Google HTML/CSS Style Guide](https://google.github.io/styleguide/htmlcssguide.html)：構造・見た目・振る舞いの分離とHTML/CSSの一貫性
- [MDN: JavaScript modules](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Modules)：モジュールによる責務分割
- [MDN: Control flow and error handling](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Control_flow_and_error_handling)：制御フローとエラー処理
- [MDN: Structuring content with HTML](https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/Structuring_content)：意味のあるHTML構造
- [MDN: HTML accessibility](https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/Accessibility/HTML)：アクセシブルなHTML
- [ESLint: Core concepts](https://eslint.org/docs/latest/use/core-concepts/)：静的解析による規約と問題の検出
- [Martin Fowler: Refactoring](https://refactoring.com/)：動作を保ちながら内部構造を改善する考え方
- [Martin Fowler: Code Smell](https://martinfowler.com/bliki/CodeSmell.html)：機械的な規則ではなく、設計上の問題を検討する兆候

## まとめ

このプロジェクトでは、好みだけで「きれい」「汚い」を判断しません。責務が明確か、意図を説明できるか、一貫しているか、変更後に検証できるかを基準にします。

規約の目的はコードを同じ形に押し込むことではなく、第三者が変更の意図と影響を理解し、安心して次の変更を加えられる状態を保つことです。
